title: ItemImportRequestOptions
type: object
properties:
  webhook:
    type: string
    description: >-
      Specifies a webhook URL to associate with an Item. Plaid fires a webhook
      if credentials fail.
description: An optional object to configure `/item/import` request.
