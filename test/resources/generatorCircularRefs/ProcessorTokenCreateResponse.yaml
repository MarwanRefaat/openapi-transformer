title: ProcessorTokenCreateResponse
required:
  - processor_token
  - request_id
type: object
properties:
  processor_token:
    type: string
    description: >-
      The `processor_token` that can then be used by the Plaid partner to make
      API requests
  request_id:
    type: string
    description: >-
      A unique identifier for the request, which can be used for
      troubleshooting. This identifier, like all Plaid identifiers, is case
      sensitive.
description: >-
  ProcessorTokenCreateResponse defines the response schema for
  `/processor/token/create` and `/processor/apex/processor_token/create`
