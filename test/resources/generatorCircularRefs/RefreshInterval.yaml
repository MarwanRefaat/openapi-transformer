title: RefreshInterval
type: string
description: >-
  The `refresh_interval` may be `DELAYED` or `STOPPED` even when the success
  rate is high. This value is only returned for Transactions status breakdowns.
enum:
  - NORMAL
  - DELAYED
  - STOPPED
